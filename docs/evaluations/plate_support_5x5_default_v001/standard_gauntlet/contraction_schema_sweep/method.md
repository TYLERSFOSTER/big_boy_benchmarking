# PlateSupport Contraction Schema Sweep Method

The sweep consumes Stage 1 structural diagnostics, runs the mandatory
no-contraction and upstream-default schema modes, and emits explicit
unsupported rows for custom schema families that the current upstream
PlateSupport probe cannot represent honestly.
