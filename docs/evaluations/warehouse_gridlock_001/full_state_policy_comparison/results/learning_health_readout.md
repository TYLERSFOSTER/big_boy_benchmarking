# Learning Health Readout

The key table is `results/learning_health_summary.csv`.

A run with update rows but no prior learned signal reuse is classified as
`nominal_updates_only`, not as successful training.
