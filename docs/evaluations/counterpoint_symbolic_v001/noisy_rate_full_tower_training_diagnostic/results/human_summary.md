# Human Summary

The artifact set is `complete`. It covers `7` selected non-collapsed noisy-rate tower candidates. In this run, those candidates emitted `222` concrete steps and `313` successful learner updates. The observed training health classes were `{'trainable_clean': 7}`.

Interpret this as an executability-and-training-health diagnostic, not as a performance comparison.
