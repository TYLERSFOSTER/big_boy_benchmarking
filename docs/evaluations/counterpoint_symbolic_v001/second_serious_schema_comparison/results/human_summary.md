# Human Summary

The engineering repair succeeded: the run now constructs the full iterated
Schema 1 tower rather than stopping at the one-drop `[108,54]` source. The
observed tower is `[108,54,27,19,14]`.

The learning result is not positive. Under the current short smoke budget and
threshold `R = 13.0`, Schema 0 only hits transiently and Schema 1 never hits.
This is diagnostic evidence, not a speed-to-hit comparison claim.
