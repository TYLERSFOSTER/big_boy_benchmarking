# Method

Evaluation id: `counterpoint_contraction_fraction_sweep_diagnostics_v001`.

The sweep runs the existing counterpoint hidden graph with one scheduled source-local outgoing-edge contraction block per fraction arm.
The required arms are `1/18` through `6/18`; `6/18` must be checked against the old one-third first block.

Current scope: `smoke`.
Instances: `counterpoint_symbolic_n3_small_v001`.
Numerators: `1`, `6`.
Schema seeds: `0`.
Replicates per schema seed: `1`.
Episodes per replicate: `1`.
Linearization mode: `tensor_available_disabled`.

Current artifact run label:

```text
smoke_001
```
