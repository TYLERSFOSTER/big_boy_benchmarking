# Sweep Verdict

Verdict: `immediate_collapse`.

Interpretation: this smoke run reports `immediate collapse`. The first full-collapse numerator is `1`, and the last nontrivial numerator is `none observed`.
