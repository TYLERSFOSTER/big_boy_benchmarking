# Results Summary

No durable checked-in result artifact set is declared by this page.

Run-specific generated summaries live under:

```text
<artifact-root>/evaluations/counterpoint_first_serious_learning_v001/docs/results/summary.md
```

The corresponding machine-readable aggregate table lives at:

```text
<artifact-root>/evaluations/counterpoint_first_serious_learning_v001/evaluation_aggregate_table.csv
```

Only promote a result summary into `docs/results/` when the artifact root and
claim boundary are intentionally durable.
